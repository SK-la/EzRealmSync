<UserControl x:Class="osu.EzRealmSync.Desktop.Pages.DataPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
  <Grid Margin="4" VerticalAlignment="Stretch">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" MinHeight="120" />
    </Grid.RowDefinitions>

    <ui:Card Grid.Row="0" Padding="14" Margin="0,0,0,10">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ComboBox x:Name="RealmSelectCombo"
                  MinWidth="280"
                  MinHeight="36"
                  VerticalContentAlignment="Center"
                  DisplayMemberPath="DisplayName"
                  SelectedValuePath="Id"
                  SelectionChanged="RealmSelectCombo_OnChanged" />
        <ui:Button x:Name="LoadButton"
                   Grid.Column="1"
                   Margin="12,0,0,0"
                   MinHeight="36"
                   Click="Load_OnClick"
                   Appearance="Primary"
                   Icon="{ui:SymbolIcon Database24}" />
      </Grid>
    </ui:Card>

    <ui:TextBlock x:Name="SummaryText"
                  Grid.Row="1"
                  Style="{StaticResource WorkspaceMutedText}"
                  Margin="4,0,4,8"
                  TextWrapping="Wrap" />

    <ui:Card Grid.Row="2"
             Style="{StaticResource WorkspaceStretchCard}"
             Padding="8">
      <Grid VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="220" />
          <ColumnDefinition Width="8" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" LastChildFill="True">
          <ui:TextBlock x:Name="ClassesHeader"
                        DockPanel.Dock="Top"
                        Style="{StaticResource WorkspaceBodyText}"
                        FontWeight="SemiBold"
                        Margin="4,0,0,8" />
          <ListBox x:Name="ClassList"
                   Style="{StaticResource WorkspaceClassList}"
                   ItemContainerStyle="{StaticResource WorkspaceClassListItem}"
                   SelectionChanged="ClassList_OnSelectionChanged">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <Grid MinHeight="28" Margin="2,0">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <TextBlock Text="{Binding DisplayName}"
                             TextTrimming="CharacterEllipsis"
                             VerticalAlignment="Center" />
                  <Border Grid.Column="1"
                          Margin="8,0,0,0"
                          Padding="8,2"
                          CornerRadius="4"
                          Background="#33FFFFFF"
                          VerticalAlignment="Center">
                    <TextBlock Text="{Binding CountDisplay}"
                               FontSize="12"
                               VerticalAlignment="Center" />
                  </Border>
                </Grid>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>

        <ui:DataGrid x:Name="DataGrid"
                     Grid.Column="2"
                     Style="{StaticResource WorkspaceStretchDataGrid}"
                     AutoGenerateColumns="False"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     IsReadOnly="True"
                     SelectionMode="Extended" />
      </Grid>
    </ui:Card>
  </Grid>
</UserControl>
