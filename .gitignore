bin/
obj/
.vs/

# 发布暂存目录（release.yml 用 artifacts/publish）
artifacts/

# 可选本地 osu 构建产物（见 lib/README.md）；NuGet 模式不需要
lib/*.dll
lib/**/*.dll
lib/runtimes/

# 运行时日志（exe/log/）
log/

# 测试 blame / 覆盖率产物
**/TestResults/

*.user
*.suo
*.DotSettings.user

# 本地复制的 Noto 字体（见 scripts/copy-cjk-fonts.ps1）
osu.EzRealmSync/Assets/Fonts/Noto/

# Realm 样本仅用于本地测试，不提交到仓库
*.realm
